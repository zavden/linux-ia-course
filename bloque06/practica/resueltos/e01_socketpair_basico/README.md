# E01 — socketpair básico

## Objetivo
Entender comunicación full-duplex entre dos extremos de socket en el mismo proceso.

## Qué hace
- Crea un `socketpair` local.
- Envía `ping` de un extremo al otro.
- Responde `pong` en sentido inverso.

## Ejecutar
```bash
make run
make test
```
