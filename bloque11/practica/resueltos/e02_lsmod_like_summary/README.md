# E02 - resumen tipo lsmod

## Objetivo
Practicar parseo de tabla `lsmod` para construir un resumen de carga de módulos.

## Qué hace
- Lee salida estilo `lsmod`.
- Cuenta módulos y cuántos tienen `Used by > 0`.
- Detecta módulo de mayor tamaño.

## Ejecutar
```bash
make run
make test
```
