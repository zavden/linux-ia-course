# C03 — shell con pipes y redirecciones (guiado)

## Objetivo
Implementar ejecución con `|`, `>`, `>>`, `<` y combinación básica.

## Qué debe hacer
- Parsear una línea con dos comandos en pipeline.
- Aplicar redirecciones de entrada/salida.
- Ejecutar con `fork`, `pipe`, `dup2`, `execvp`.
- Manejar errores sin matar shell abruptamente.

## Estado
Plantilla guiada (sin resolver completamente).
