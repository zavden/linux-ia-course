#include <stdio.h>
#include <stdlib.h>

// TODO: struct de nuestro array y len y capacity
typedef struct {
    int *data;
    size_t len;
    size_t capacity;
} dyn_array;

// TODO: Funcion init_array
// TODO: Funcion push_array() (que use realloc() al chocar la capacity)
// TODO: Funcion free_array() (que libere el puntero interno)

int main(void) {
    // 1. Inicializar lista cap 2
    // 2. Loop for (1 to 20): push() metiendo cosas
    // 3. Destruir estructura librerandola limpiamente
    
    return EXIT_SUCCESS;
}
