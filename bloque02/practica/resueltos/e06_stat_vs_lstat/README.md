# E06 — stat vs lstat

## Objetivo
Diferenciar metadatos del target (`stat`) y del enlace (`lstat`).

## Qué hace
- Recibe una ruta.
- Imprime tipo detectado por `stat` y por `lstat`.
- Muestra inodo y número de enlaces.

## Ejecutar
```bash
make run
make test
```
