# E06 — Lista enlazada genérica básica

## Objetivo
Construir una lista simple (`create/push/pop/destroy`) usando `void *`.

## Qué hace
- Inserta al frente en O(1).
- Extrae del frente en O(1).
- Libera todos los nodos al final.

## Ejecutar
```bash
make run
make test
```
