# E04 — SHA-256 básico en C

## Objetivo
Implementar hash SHA-256 puro en C para entender integridad criptográfica básica.

## Qué hace
- Calcula SHA-256 de una cadena de entrada.
- Emite digest hexadecimal.
- Verifica vector conocido (`abc`).

## Ejecutar
```bash
make run
make test
```
