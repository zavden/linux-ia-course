# E02 — macros de trazas por nivel

## Objetivo
Controlar verbosidad de logs con macros y niveles de compilación.

## Qué hace
- Define niveles `ERROR`, `INFO`, `DEBUG`.
- Emite mensajes según `LOG_LEVEL`.
- Cuenta cuántas trazas se imprimen por nivel.

## Ejecutar
```bash
make run
make test
```
